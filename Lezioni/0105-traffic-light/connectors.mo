connector InputInteger = input Integer;
connector OutputInteger = output Integer;

connector InputReal = input Real;
connector OutputReal = output Real;

connector InputBoolean = input Boolean;
connector OutputBoolean = output Boolean;

// il tipo enumerativo è un tipo vero e proprio, posso quindi utilizzarlo per gli input e per gli output
connector InputTrafficLightSignal = input TrafficLightSignal;
connector OutputTrafficLightSignal = output TrafficLightSignal;